import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'react_capacitor_ios_safezone',
  webDir: 'dist',
};

export default config;
